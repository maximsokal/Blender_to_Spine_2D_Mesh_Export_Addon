# 0.55.10

## Active Camera semantic release-contract correction

The 0.55.9 focused gate passed the corrected Z-group fixture and then stopped only because its release-note test required one exact contiguous phrase that the release note did not use.

The release note already described the required behavior through the profile-owned `z_index_base`, the resulting valid production bindings, and the removal of a zero-based index assumption. The contract now verifies those semantic facts independently instead of requiring the artificial phrase `profile owned z group indices`.

### Fixed

- The 0.55.9 release-note contract checks `profile-owned z_index_base`, valid production bindings, and the removed zero-based assumption as separate semantic requirements.
- Case, whitespace, and hyphen normalization remain explicit.

### Unchanged

- Production code is unchanged.
- The corrected 0.55.9 pure fixture is unchanged.
- Active Camera projection, depth-parent compensation, rig assembly, standalone draw order, Blender acceptance, signed-axis projection, rendered Camera Projection, connected and mixed assembly, Spine codecs, UV topology, and texture baking are unchanged.
