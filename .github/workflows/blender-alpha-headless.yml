name: Blender 4.4 Alpha Bake

# Manual-only while the rewrite branch is under active development.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: blender-alpha-headless-${{ github.ref }}
  cancel-in-progress: true

env:
  BLENDER_VERSION: "4.4.0"
  BLENDER_ARCHIVE: "blender-4.4.0-linux-x64.tar.xz"
  BLENDER_DIRECTORY: ".blender/blender-4.4.0"

jobs:
  blender-alpha-headless:
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Cache Blender 4.4
        id: blender-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.BLENDER_DIRECTORY }}
          key: blender-${{ runner.os }}-${{ runner.arch }}-${{ env.BLENDER_VERSION }}

      - name: Download official Blender archive
        if: steps.blender-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .blender/download "${BLENDER_DIRECTORY}"
          curl --fail --location --retry 5 --retry-delay 3 \
            "https://download.blender.org/release/Blender4.4/${BLENDER_ARCHIVE}" \
            --output ".blender/download/${BLENDER_ARCHIVE}"
          tar \
            --extract \
            --file ".blender/download/${BLENDER_ARCHIVE}" \
            --directory "${BLENDER_DIRECTORY}" \
            --strip-components=1
          test -x "${BLENDER_DIRECTORY}/blender"

      - name: Run alpha and transparency bake tests
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p test-results
          "${BLENDER_DIRECTORY}/blender" \
            --background \
            --factory-startup \
            --python-exit-code 1 \
            --python tests/blender_headless/run_alpha_bake_integration.py \
            2>&1 | tee test-results/blender-alpha-bake.log

      - name: Run alpha sequence nested and rollback tests
        shell: bash
        run: |
          set -euo pipefail
          "${BLENDER_DIRECTORY}/blender" \
            --background \
            --factory-startup \
            --python-exit-code 1 \
            --python tests/blender_headless/run_alpha_sequence_rollback_integration.py \
            2>&1 | tee test-results/blender-alpha-extended.log

      - name: Upload alpha bake logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: blender-4.4-alpha-bake-logs
          path: test-results/blender-alpha-*.log
          if-no-files-found: warn
          retention-days: 7
